services:
  automated-playlists-frontend:
    command: npm run dev
    environment:
      NODE_ENV: development

  automated-playlists-backend:
    command: npm run dev
    environment:
      NODE_ENV: development